<section>
  <h2>アットホストが選ばれる理由</h2>

  <ul>
    <% items.each do |item| %>
      <li>
        <h3><%= item[:title] %></h3>
        <p><%= item[:body] %></p>
      </li>
    <% end %>
  </ul>
</section>
